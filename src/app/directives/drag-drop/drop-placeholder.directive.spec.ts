import { DropPlaceholderDirective } from './drop-placeholder.directive';

describe('DropPlaceholderDirective', () => {
  it('should create an instance', () => {
    const directive = new DropPlaceholderDirective();
    expect(directive).toBeTruthy();
  });
});
